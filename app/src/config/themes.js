import * as colors from '@material-ui/core/colors';

export const themes = {
  dark: {
    palette: {
      type: 'dark',
      primary: colors.green
    },
    typography: {
      useNextVariants: true
    }
  },
  light: {
    palette: {
      type: 'light',
      primary: colors.green
    },
    typography: {
      useNextVariants: true
    }
  }
};
